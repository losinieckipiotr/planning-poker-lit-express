/**
 * TODO: for now unused, maybe will be needed for component with shadowRoot?
 */
import { unsafeCSS } from 'lit'
import globalCSS from './index.css?inline'

export const globalStyles = unsafeCSS(globalCSS)
